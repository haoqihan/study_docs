bee工具应用

```shell
bee new :新建项目目录
bee run ：自动编译部署
bee generate 自动生成代码
```

```sql
create table user(id int(11) Not null auto_increment,name varchar(128) not null default '', gender tinyint(4) not null default '0', age int(11) not null default '0',PRIMARY  KEY (id));
```

